# `data/processed/`

Intermediate/cached artifacts derived from `data/raw/` (e.g. a cleaned
and reshaped copy of a downloaded dataset, ready to feed into
`plagiarism_engine.cli pairs`) can be written here. The directory is
intentionally empty in the repository -- populate it by running your own
preprocessing scripts against the datasets described in
`data/raw/README.md`.
